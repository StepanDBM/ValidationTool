<UserControl x:Class="ValidationTool.UI.Views.StatsView_Controls.UC_FixModeByStageView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{StaticResource BrushBgPanel}"
            BorderBrush="{StaticResource BrushBorder}"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12">

        <Grid>
            <TextBlock Text="Fix Mode By Stage&#x0a;&#x0a;Stage-level comparison of Auto / Semi / Manual / None fix modes."
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       TextWrapping="Wrap"/>
        </Grid>
    </Border>
</UserControl>