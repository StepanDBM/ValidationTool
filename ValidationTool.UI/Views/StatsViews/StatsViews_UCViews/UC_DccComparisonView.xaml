<UserControl x:Class="ValidationTool.UI.Views.StatsView_Controls.UC_DccComparisonView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Border Background="{StaticResource BrushBgPanel}"
            BorderBrush="{StaticResource BrushBorder}"
            BorderThickness="1"
            CornerRadius="6"
            Padding="12">

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="6"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="12"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="DCC COMPARISON"
                       FontSize="15"
                       FontWeight="Bold"
                       Opacity="0.9"/>

            <TextBlock Grid.Row="2"
                       Text="{Binding Subtitle}"
                       FontSize="11"
                       Opacity="0.65"/>

            <ScrollViewer Grid.Row="4"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">

                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource BrushBgElement}"
                                    BorderBrush="{StaticResource BrushBorder}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="8"
                                    Margin="0,0,0,10">

                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="8"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="8"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <Grid Grid.Row="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <TextBlock Grid.Column="0"
                                                   Text="{Binding Dcc}"
                                                   FontWeight="Bold"
                                                   FontSize="14"
                                                   VerticalAlignment="Center"
                                                   TextTrimming="CharacterEllipsis"
                                                   ToolTip="{Binding Dcc}"/>

                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding TotalIssuesDisplay}"
                                                   FontWeight="Bold"
                                                   FontSize="14"
                                                   VerticalAlignment="Center"
                                                   HorizontalAlignment="Right"/>
                                    </Grid>

                                    <ProgressBar Grid.Row="2"
                                                 Height="16"
                                                 Minimum="0"
                                                 Maximum="100"
                                                 Value="{Binding IssueBarPercent}"
                                                 Background="{StaticResource BrushBgPanel}"
                                                 Foreground="{StaticResource BrushAccent}"
                                                 BorderBrush="{StaticResource BrushBorder}"
                                                 BorderThickness="1"/>

                                    <Grid Grid.Row="4">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0">
                                            <TextBlock Opacity="0.75">
                                                <Run Text="Errors: "/>
                                                <Run Text="{Binding ErrorsDisplay}"/>
                                                <Run Text="  |  "/>
                                                <Run Text="{Binding ErrorRatioDisplay}"/>
                                            </TextBlock>

                                            <TextBlock Opacity="0.75"
                                                       Margin="0,3,0,0">
                                                <Run Text="Warnings: "/>
                                                <Run Text="{Binding WarningsDisplay}"/>
                                                <Run Text="  |  "/>
                                                <Run Text="{Binding WarningRatioDisplay}"/>
                                            </TextBlock>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1"
                                                    HorizontalAlignment="Right">
                                            <TextBlock Opacity="0.75"
                                                       HorizontalAlignment="Right">
                                                <Run Text="Files: "/>
                                                <Run Text="{Binding FileCountDisplay}"/>
                                            </TextBlock>

                                            <TextBlock Opacity="0.75"
                                                       Margin="0,3,0,0"
                                                       HorizontalAlignment="Right">
                                                <Run Text="Avg ERR/File: "/>
                                                <Run Text="{Binding AverageErrorsPerFileDisplay}"/>
                                            </TextBlock>
                                        </StackPanel>
                                    </Grid>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>
    </Border>
</UserControl>