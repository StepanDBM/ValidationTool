<UserControl x:Class="ValidationTool.UI.Views.ValidationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Button Content="Load Report"
                Click="loadReportUI"
                Width="150"
                Height="35"/>

        <TreeView
            Grid.Row="1"
            ItemsSource="{Binding Assets}">
            <TreeView.ItemTemplate>

                <HierarchicalDataTemplate ItemsSource="{Binding Stages}">
                    <TextBlock Text="{Binding AssetName}" />

                    <HierarchicalDataTemplate.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Issues}">
                            <TextBlock Text="{Binding StageName}" />

                            <HierarchicalDataTemplate.ItemTemplate>
                                <DataTemplate>
                                    <StackPanel Orientation="Horizontal">

                                        <TextBlock Text="{Binding CheckName}" Width="150"/>
                                        <TextBlock Text="{Binding Severity}" Width="100"/>
                                        <TextBlock Text="{Binding Message}" />

                                    </StackPanel>
                                </DataTemplate>
                            </HierarchicalDataTemplate.ItemTemplate>

                        </HierarchicalDataTemplate>
                    </HierarchicalDataTemplate.ItemTemplate>

                </HierarchicalDataTemplate>

            </TreeView.ItemTemplate>
        </TreeView>

    </Grid>

</UserControl>